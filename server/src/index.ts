import express from 'express'; import cors from 'cors'; import { createServer } from 'node:http'; import { Server } from 'socket.io'; import { config } from './config.js'; import authRoutes from './routes/auth.js'; import apiRoutes from './routes/api.js'; import { processLimits } from './services/orders.js'; import { tick } from './services/market.js';
const app=express(); app.use(cors({origin:config.CLIENT_URL})); app.use(express.json()); app.get('/health',(_req,res)=>res.json({ok:true})); app.use('/api/auth',authRoutes); app.use('/api',apiRoutes); app.use((err:Error,_req:express.Request,res:express.Response,_next:express.NextFunction)=>res.status(500).json({error:err.message}));
const server=createServer(app); const io=new Server(server,{cors:{origin:config.CLIENT_URL}}); setInterval(async()=>{const quotes=tick();io.emit('prices',quotes);await processLimits();io.emit('refresh');},1500); server.listen(config.PORT,()=>console.log(`FolioX API on ${config.PORT}`));
