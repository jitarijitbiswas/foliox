import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';

import '../../../app/primary_footer.dart';

import '../../analytics/domain/trading_analytics.dart';
import '../../trading/data/local_account_store.dart';
import '../../trading/presentation/trading_controller.dart';
import '../data/practice_repository.dart';
import '../domain/practice_engines.dart';
import '../domain/practice_models.dart';

const _green=Color(0xFF28D17C),_red=Color(0xFFFF5468),_amber=Color(0xFFFFB347),_cyan=Color(0xFF36C8E8);

class JournalScreen extends ConsumerStatefulWidget {const JournalScreen({super.key});@override ConsumerState<JournalScreen> createState()=>_JournalScreenState();}
class _JournalScreenState extends ConsumerState<JournalScreen>{String _tab='All',_search='';int _refresh=0;@override Widget build(BuildContext c){_refresh;final repo=ref.read(practiceRepositoryProvider);final snap=ref.watch(tradingControllerProvider).snapshot;final trades=TradingAnalyticsEngine.completedTrades(snap?.orders??[]);var entries=repo.journals();entries=entries.where((e)=>_search.isEmpty||'${e.symbol} ${e.setupName} ${e.tags.join(' ')} ${e.entryReason}'.toLowerCase().contains(_search.toLowerCase())).toList();entries=entries.where((e){final t=trades.where((x)=>x.id==e.tradeId).firstOrNull;if(_tab=='Open')return e.tradeId==null;if(_tab=='Closed')return e.tradeId!=null;if(_tab=='Wins')return t?.realizedPnl!=null&&t!.realizedPnl>0;if(_tab=='Losses')return t?.realizedPnl!=null&&t!.realizedPnl<0;return true;}).toList();final rate=trades.isEmpty?0.0:repo.journals().where((e)=>e.tradeId!=null).length/trades.length*100;return Scaffold(appBar:AppBar(title:const Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('Trade Journal'),Text('Review. Learn. Improve.',style:TextStyle(fontSize:12,color:Colors.white60,fontWeight:FontWeight.normal))]),actions:[IconButton(onPressed:()=>_editor(c,null,trades),icon:const Icon(Icons.add))]),bottomNavigationBar:const PrimaryFooter(selectedIndex:1),body:RefreshIndicator(onRefresh:()async=>setState(()=>_refresh++),child:ListView(padding:const EdgeInsets.fromLTRB(16,12,16,30),children:[_overview(entries.length,rate),const SizedBox(height:16),TextField(onChanged:(x)=>setState(()=>_search=x),decoration:const InputDecoration(prefixIcon:Icon(Icons.search),hintText:'Search symbol, setup, tag or note')),const SizedBox(height:12),_tabs(_tab,(x)=>setState(()=>_tab=x),const ['All','Open','Closed','Wins','Losses']),const SizedBox(height:14),if(entries.isEmpty)_journalEmpty(c,trades) else ...entries.map((e)=>_journalCard(c,e,trades))])));}
  Widget _overview(int count,double rate)=>_box(child:Row(mainAxisAlignment:MainAxisAlignment.spaceAround,children:[_stat('$count','Trades Reviewed'),_stat('${rate.round()}%','Review Rate'),_stat('Local','Private notes')]));
  Widget _journalCard(BuildContext c,TradeJournalEntry e,List<CompletedTrade> ts){final t=ts.where((x)=>x.id==e.tradeId).firstOrNull;final pnl=t?.realizedPnl;return Card(margin:const EdgeInsets.only(bottom:9),child:ListTile(onTap:()=>_editor(c,e,ts),title:Row(children:[Expanded(child:Text(e.symbol,style:const TextStyle(fontWeight:FontWeight.w800))),if(pnl!=null)Text(_signed(pnl,e.symbol),style:TextStyle(color:pnl>=0?_green:_red,fontWeight:FontWeight.w800))]),subtitle:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[if(e.setupName.isNotEmpty)Text('Setup · ${e.setupName}'),if(e.confidence!=null)Text('Confidence ${'★'*e.confidence!}${'☆'*(5-e.confidence!)}'),if(e.entryReason.isNotEmpty)Text(e.entryReason,maxLines:2,overflow:TextOverflow.ellipsis),Text('${e.createdAt.day}/${e.createdAt.month} · Review →',style:const TextStyle(color:Colors.white54))]) ));}
  Widget _journalEmpty(BuildContext c,List<CompletedTrade> trades)=>Padding(padding:const EdgeInsets.only(top:80),child:Column(children:[const Icon(Icons.menu_book_outlined,size:52,color:_cyan),const SizedBox(height:16),Text('Your Trading Journal',style:Theme.of(c).textTheme.headlineSmall),const SizedBox(height:8),Text(trades.isEmpty?'Complete a trade and review it to start learning from your decisions.':'Turn every trade into a lesson.',textAlign:TextAlign.center),const SizedBox(height:18),FilledButton(onPressed:trades.isEmpty?null:()=>_editor(c,null,trades),child:const Text('Review a Trade'))]));
  Future<void> _editor(BuildContext c,TradeJournalEntry? current,List<CompletedTrade> trades)async{final result=await showModalBottomSheet<bool>(context:c,isScrollControlled:true,builder:(x)=>_JournalEditor(entry:current,trades:trades,setups:ref.read(practiceRepositoryProvider).setups()));if(result==true&&mounted)setState(()=>_refresh++);}
}
class _JournalEditor extends ConsumerStatefulWidget{const _JournalEditor({this.entry,required this.trades,required this.setups});final TradeJournalEntry? entry;final List<CompletedTrade> trades;final List<TradingSetup> setups;@override ConsumerState<_JournalEditor>createState()=>_JournalEditorState();}
class _JournalEditorState extends ConsumerState<_JournalEditor>{late final TextEditingController _reason,_expect,_lesson,_tags;String? _trade,_setup,_mindset;int _confidence=3;bool? _plan;@override void initState(){super.initState();final e=widget.entry;_trade=e?.tradeId;_setup=e?.setupId;_mindset=e?.mindset;_confidence=e?.confidence??3;_plan=e?.followedPlan;_reason=TextEditingController(text:e?.entryReason);_expect=TextEditingController(text:e?.expectation);_lesson=TextEditingController(text:e?.lesson);_tags=TextEditingController(text:e?.tags.join(' '));}@override void dispose(){_reason.dispose();_expect.dispose();_lesson.dispose();_tags.dispose();super.dispose();}@override Widget build(BuildContext c){final selected=widget.trades.where((x)=>x.id==_trade).firstOrNull;return Padding(padding:EdgeInsets.fromLTRB(20,20,20,MediaQuery.viewInsetsOf(c).bottom+24),child:SingleChildScrollView(child:Column(mainAxisSize:MainAxisSize.min,crossAxisAlignment:CrossAxisAlignment.start,children:[Text(widget.entry==null?'Review Trade':'Edit Trade Review',style:Theme.of(c).textTheme.titleLarge),const SizedBox(height:14),DropdownButtonFormField<String>(value:_trade,decoration:const InputDecoration(labelText:'Completed trade'),items:widget.trades.map((t)=>DropdownMenuItem(value:t.id,child:Text('${t.symbol} · ${_signed(t.realizedPnl,t.symbol)}'))).toList(),onChanged:(x)=>setState(()=>_trade=x)),const SizedBox(height:10),DropdownButtonFormField<String>(value:_setup,decoration:const InputDecoration(labelText:'Trade setup'),items:[const DropdownMenuItem(value:null,child:Text('No setup yet')),...widget.setups.map((s)=>DropdownMenuItem(value:s.id,child:Text(s.name)))],onChanged:(x)=>setState(()=>_setup=x)),const SizedBox(height:10),TextField(controller:_reason,decoration:const InputDecoration(labelText:'Why I entered',hintText:'What was the setup?'),maxLines:2),const SizedBox(height:10),TextField(controller:_expect,decoration:const InputDecoration(labelText:'What I expected'),maxLines:2),const SizedBox(height:10),Text('Confidence before trade · $_confidence / 5'),Slider(value:_confidence.toDouble(),min:1,max:5,divisions:4,label:'$_confidence',onChanged:(v)=>setState(()=>_confidence=v.round())),DropdownButtonFormField<String>(value:_mindset,decoration:const InputDecoration(labelText:'Mindset (optional)'),items:['Calm','Confident','Neutral','Fearful','Greedy','FOMO','Impatient','Revenge'].map((x)=>DropdownMenuItem(value:x,child:Text(x))).toList(),onChanged:(x)=>setState(()=>_mindset=x)),const SizedBox(height:10),Text('Did you follow your plan?'),Row(children:['Yes','Partially','No'].asMap().entries.map((x)=>Expanded(child:ChoiceChip(label:Text(x.value),selected:_plan==(x.key==0?true:x.key==2?false:null),onSelected:(_)=>setState(()=>_plan=x.key==0?true:x.key==2?false:null)))).toList()),const SizedBox(height:10),TextField(controller:_lesson,decoration:const InputDecoration(labelText:'Key Lesson',hintText:'What will you do differently next time?'),maxLines:2),const SizedBox(height:10),TextField(controller:_tags,decoration:const InputDecoration(labelText:'Tags',hintText:'#breakout #volume')),const SizedBox(height:18),Row(children:[if(widget.entry!=null)TextButton(onPressed:()async{await ref.read(practiceRepositoryProvider).deleteJournal(widget.entry!.id);if(c.mounted)Navigator.pop(c,true);},child:const Text('Delete',style:TextStyle(color:_red))),const Spacer(),FilledButton(onPressed:selected==null?null:()async{final setup=widget.setups.where((s)=>s.id==_setup).firstOrNull;final now=DateTime.now();await ref.read(practiceRepositoryProvider).saveJournal(TradeJournalEntry(id:widget.entry?.id??now.microsecondsSinceEpoch.toString(),symbol:selected.symbol,tradeId:selected.id,setupId:setup?.id,setupName:setup?.name??'',entryReason:_reason.text,expectation:_expect.text,lesson:_lesson.text,confidence:_confidence,mindset:_mindset??'',followedPlan:_plan,tags:_tags.text.split(RegExp(r'\s+')).where((x)=>x.isNotEmpty).toList(),createdAt:widget.entry?.createdAt??now,updatedAt:now));if(c.mounted)Navigator.pop(c,true);},child:const Text('Save Review'))])])));}
}

class RiskScreen extends ConsumerStatefulWidget{const RiskScreen({super.key});@override ConsumerState<RiskScreen>createState()=>_RiskScreenState();}class _RiskScreenState extends ConsumerState<RiskScreen>{double _move=-5;int _refresh=0;@override Widget build(BuildContext c){_refresh;final p=ref.watch(tradingControllerProvider).snapshot?.portfolio;if(p==null)return const Scaffold(body:Center(child:CircularProgressIndicator()));final repo=ref.read(practiceRepositoryProvider);final settings=repo.riskSettings();final risk=RiskManagementEngine.calculate(p,settings);final status=risk.score<30?'Low':risk.score<55?'Moderate':risk.score<75?'Elevated':'High';final impact=RiskManagementEngine.scenarioImpact(p,_move);return Scaffold(appBar:AppBar(title:const Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('Risk Management'),Text('Know your exposure before you trade.',style:TextStyle(fontSize:12,color:Colors.white60,fontWeight:FontWeight.normal))])),body:ListView(padding:const EdgeInsets.fromLTRB(16,12,16,30),children:[_box(child:Row(children:[SizedBox(width:80,height:80,child:Stack(fit:StackFit.expand,children:[CircularProgressIndicator(value:risk.score/100,strokeWidth:8,color:risk.score<55?_amber:_red,backgroundColor:const Color(0xFF263244)),Center(child:Text('${risk.score}'))])),const SizedBox(width:18),Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Portfolio Risk',style:TextStyle(color:Colors.white60)),Text(status,style:Theme.of(c).textTheme.headlineSmall),const Text('Educational learning metric')])])),const SizedBox(height:20),const Text('Capital',style:TextStyle(fontWeight:FontWeight.w800,fontSize:17)),_box(child:Column(children:[_row('Starting',_money(LocalAccountStore.initialBalance)),_row('Invested',_money(p.positions.fold(0.0,(s,x)=>s+x.ltp*x.quantity))),_row('Available',_money(p.cashBalance)),const SizedBox(height:8),LinearProgressIndicator(value:(risk.utilization/100).clamp(0,1),minHeight:8,borderRadius:BorderRadius.circular(6)),const SizedBox(height:5),Text('${risk.utilization.toStringAsFixed(1)}% capital utilized',style:const TextStyle(color:Colors.white60,fontSize:12))])),const SizedBox(height:20),const Text('Position Exposure',style:TextStyle(fontWeight:FontWeight.w800,fontSize:17)),if(p.positions.isEmpty)_box(child:const Text('No Open Risk\nYour portfolio currently has no market exposure.')) else ...([...p.positions]..sort((a,b)=>(b.ltp*b.quantity).compareTo(a.ltp*a.quantity))).map((x)=>_box(child:ListTile(onTap:()=>c.push('/stock/${Uri.encodeComponent(x.symbol)}'),contentPadding:EdgeInsets.zero,title:Text(x.symbol),subtitle:Text('Unrealized ${_signed(x.unrealizedPnl,x.symbol)}'),trailing:Text('${(x.ltp*x.quantity/p.equity*100).toStringAsFixed(1)}%')))),const SizedBox(height:12),_box(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Risk Limits',style:TextStyle(fontWeight:FontWeight.w800)),_limit(c,'Maximum Daily Loss',_money(settings.maxDailyLoss),settings,(s)=>s),_limit(c,'Max position size','${settings.maxPositionSizePercent.toStringAsFixed(0)}%',settings,(s)=>s),_limit(c,'Max capital utilization','${settings.maxCapitalUtilizationPercent.toStringAsFixed(0)}%',settings,(s)=>s)])),const SizedBox(height:18),const Text('What If?',style:TextStyle(fontWeight:FontWeight.w800,fontSize:17)),_box(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('Market moves ${_move.toStringAsFixed(0)}%',style:const TextStyle(fontWeight:FontWeight.w700)),Slider(value:_move,min:-10,max:10,divisions:8,label:'${_move.toStringAsFixed(0)}%',onChanged:(x)=>setState(()=>_move=x)),Text('Estimated portfolio impact ${_signed(impact)}',style:TextStyle(color:impact>=0?_green:_red,fontWeight:FontWeight.w800)),Text('Estimated portfolio value ${_money(p.equity+impact)}\nIllustrative scenario — not a prediction.',style:const TextStyle(color:Colors.white60))])),const SizedBox(height:18),const Text('Risk Alerts',style:TextStyle(fontWeight:FontWeight.w800,fontSize:17)),...risk.alerts.map((x)=>_box(child:Row(children:[Icon(x.startsWith('Current')?Icons.check_circle_outline:Icons.warning_amber_rounded,color:x.startsWith('Current')?_green:_amber),const SizedBox(width:12),Expanded(child:Text(x))])))]));}
 Widget _limit(BuildContext c,String label,String value,RiskSettings settings,RiskSettings Function(RiskSettings) copy)=>ListTile(contentPadding:EdgeInsets.zero,title:Text(label),trailing:TextButton(onPressed:()=>_editLimit(c,label,value,settings),child:Text(value)));
 Future<void>_editLimit(BuildContext c,String label,String value,RiskSettings current)async{final ctl=TextEditingController(text:value.replaceAll(RegExp('[^0-9.]'),''));final saved=await showModalBottomSheet<bool>(context:c,builder:(x)=>Padding(padding:EdgeInsets.fromLTRB(24,20,24,MediaQuery.viewInsetsOf(x).bottom+24),child:Column(mainAxisSize:MainAxisSize.min,children:[Text(label,style:Theme.of(x).textTheme.titleLarge),const SizedBox(height:12),TextField(controller:ctl,keyboardType:const TextInputType.numberWithOptions(decimal:true),decoration:const InputDecoration(labelText:'Value')),const SizedBox(height:16),FilledButton(onPressed:()async{final n=double.tryParse(ctl.text);if(n==null)return;var next=current;if(label=='Maximum Daily Loss')next=RiskSettings(maxDailyLoss:n,maxPositionSizePercent:current.maxPositionSizePercent,maxOpenPositions:current.maxOpenPositions,maxCapitalUtilizationPercent:current.maxCapitalUtilizationPercent,maxSectorExposurePercent:current.maxSectorExposurePercent);if(label=='Max position size')next=RiskSettings(maxDailyLoss:current.maxDailyLoss,maxPositionSizePercent:n,maxOpenPositions:current.maxOpenPositions,maxCapitalUtilizationPercent:current.maxCapitalUtilizationPercent,maxSectorExposurePercent:current.maxSectorExposurePercent);if(label=='Max capital utilization')next=RiskSettings(maxDailyLoss:current.maxDailyLoss,maxPositionSizePercent:current.maxPositionSizePercent,maxOpenPositions:current.maxOpenPositions,maxCapitalUtilizationPercent:n,maxSectorExposurePercent:current.maxSectorExposurePercent);await ref.read(practiceRepositoryProvider).saveRisk(next);if(x.mounted)Navigator.pop(x,true);},child:const Text('Save'))])));if(saved==true)setState(()=>_refresh++);}
}

class StrategiesScreen extends ConsumerStatefulWidget{const StrategiesScreen({super.key});@override ConsumerState<StrategiesScreen>createState()=>_StrategiesScreenState();}class _StrategiesScreenState extends ConsumerState<StrategiesScreen>{int _refresh=0;@override Widget build(BuildContext c){_refresh;final repo=ref.read(practiceRepositoryProvider);final perf=StrategyAnalyticsEngine.calculate(repo.setups(),repo.journals(),ref.watch(tradingControllerProvider).snapshot?.orders??[]);final best=perf.isEmpty?null:perf.first;return Scaffold(appBar:AppBar(title:const Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('Strategy Analytics'),Text('Discover what works for you.',style:TextStyle(fontSize:12,color:Colors.white60,fontWeight:FontWeight.normal))]),actions:[IconButton(onPressed:()=>_newSetup(c),icon:const Icon(Icons.add))]),body:ListView(padding:const EdgeInsets.fromLTRB(16,12,16,30),children:[_box(child:Row(mainAxisAlignment:MainAxisAlignment.spaceAround,children:[_stat('${perf.length}','Setups Tracked'),_stat(best?.setup.name??'—','Best Setup'),_stat(best==null?'—':'${best.winRate.round()}%','Best Win Rate')])),const SizedBox(height:20),const Text('Your Setups',style:TextStyle(fontWeight:FontWeight.w800,fontSize:17)),if(perf.isEmpty)Padding(padding:const EdgeInsets.only(top:70),child:Column(children:[const Icon(Icons.tune_outlined,size:52,color:_cyan),const SizedBox(height:12),Text('No Setups Yet',style:Theme.of(c).textTheme.headlineSmall),const Text('Create a setup, then tag your reviewed trades to discover what works best.',textAlign:TextAlign.center),const SizedBox(height:16),FilledButton(onPressed:()=>_newSetup(c),child:const Text('Create Setup'))])) else ...perf.map((x)=>Card(margin:const EdgeInsets.only(top:9),child:ListTile(onTap:()=>_detail(c,x),title:Text(x.setup.name,style:const TextStyle(fontWeight:FontWeight.w800)),subtitle:Text('${x.count} trades · ${x.sample}\n${x.winRate.toStringAsFixed(1)}% win rate'),trailing:Column(mainAxisAlignment:MainAxisAlignment.center,crossAxisAlignment:CrossAxisAlignment.end,children:[Text(_signed(x.averagePnl),style:TextStyle(color:x.averagePnl>=0?_green:_red,fontWeight:FontWeight.w800)),const Text('Avg P&L',style:TextStyle(fontSize:11,color:Colors.white60))]))))]));}
 Future<void>_newSetup(BuildContext c)async{final name=TextEditingController(),desc=TextEditingController(),tags=TextEditingController();final ok=await showModalBottomSheet<bool>(context:c,isScrollControlled:true,builder:(x)=>Padding(padding:EdgeInsets.fromLTRB(24,20,24,MediaQuery.viewInsetsOf(x).bottom+24),child:Column(mainAxisSize:MainAxisSize.min,children:[Text('Create Setup',style:Theme.of(x).textTheme.titleLarge),const SizedBox(height:12),TextField(controller:name,decoration:const InputDecoration(labelText:'Setup Name')),const SizedBox(height:10),TextField(controller:desc,decoration:const InputDecoration(labelText:'Description')),const SizedBox(height:10),TextField(controller:tags,decoration:const InputDecoration(labelText:'Tags')),const SizedBox(height:16),FilledButton(onPressed:()async{if(name.text.trim().isEmpty)return;final now=DateTime.now();await ref.read(practiceRepositoryProvider).saveSetup(TradingSetup(id:now.microsecondsSinceEpoch.toString(),name:name.text.trim(),description:desc.text,tags:tags.text.split(RegExp(r'\s+')).where((x)=>x.isNotEmpty).toList(),createdAt:now));if(x.mounted)Navigator.pop(x,true);},child:const Text('Save Setup'))])));if(ok==true&&mounted)setState(()=>_refresh++);}
 void _detail(BuildContext c,SetupPerformance p)=>showModalBottomSheet(context:c,builder:(x)=>Padding(padding:const EdgeInsets.all(24),child:Column(mainAxisSize:MainAxisSize.min,crossAxisAlignment:CrossAxisAlignment.start,children:[Text('${p.setup.name} Analytics',style:Theme.of(x).textTheme.titleLarge),const SizedBox(height:14),_row('Trades','${p.count}'),_row('Wins / Losses','${p.winners} / ${p.losers}'),_row('Win Rate','${p.winRate.toStringAsFixed(1)}%'),_row('Net P&L',_signed(p.netPnl)),_row('Average P&L',_signed(p.averagePnl)),_row('Profit Factor',p.profitFactor?.toStringAsFixed(2)??'N/A'),const SizedBox(height:12),Text(p.sample,style:const TextStyle(color:Colors.white60))])));
}
extension<T> on Iterable<T>{T?get firstOrNull=>isEmpty?null:first;}
Widget _box({required Widget child})=>Container(margin:const EdgeInsets.only(bottom:8),padding:const EdgeInsets.all(15),decoration:BoxDecoration(color:const Color(0xFF101722),borderRadius:BorderRadius.circular(14),border:Border.all(color:const Color(0xFF263244))),child:child);Widget _stat(String a,String b)=>Column(children:[Text(a,style:const TextStyle(fontWeight:FontWeight.w800,fontSize:16)),Text(b,style:const TextStyle(color:Colors.white60,fontSize:11),textAlign:TextAlign.center)]);Widget _row(String a,String b)=>Padding(padding:const EdgeInsets.symmetric(vertical:5),child:Row(children:[Text(a,style:const TextStyle(color:Colors.white60)),const Spacer(),Text(b,style:const TextStyle(fontWeight:FontWeight.w700))]));Widget _tabs(String selected,ValueChanged<String> change,List<String> xs)=>SingleChildScrollView(scrollDirection:Axis.horizontal,child:SegmentedButton<String>(showSelectedIcon:false,segments:xs.map((x)=>ButtonSegment(value:x,label:Text(x))).toList(),selected:{selected},onSelectionChanged:(x)=>change(x.first)));String _money(num n,[String s=''])=>'${(s=='BTCUSD'||s=='XAUUSD')?r'$':'₹'}${n.abs().toStringAsFixed(2)}';String _signed(num n,[String s=''])=>'${n>=0?'+':'-'}${_money(n,s)}';
